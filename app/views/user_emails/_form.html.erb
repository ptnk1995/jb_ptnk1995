<%= form_for(@user_email) do |f| %>
  <% if @user_email.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_email.errors.count, "error") %> prohibited this user_email from being saved:</h2>

      <ul>
      <% @user_email.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_field :email %>
  </div>
  <div class="field">
    <%= f.label :is_default %><br>
    <%= f.check_box :is_default %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
